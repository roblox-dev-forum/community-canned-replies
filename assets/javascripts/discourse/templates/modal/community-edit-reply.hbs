{{#d-modal-body title="community_canned_replies.edit.modal_title" class="community-canned-replies-modal"}}
  {{community-canned-replies-form title=replyTitle content=replyContent}}
{{/d-modal-body}}

<div class="modal-footer community-canned-replies-footer">
  {{d-button class="btn-primary edit-reply-save-btn"
      action=(action "save")
      label=savingLabel
      disabled=disableSaveButton}}

  <a {{action "cancel"}}>{{i18n 'cancel'}}</a>

  {{d-button class="btn-danger pull-right"
      action=(action "remove")
      icon="trash-alt"
      disabled=saving}}
</div>
